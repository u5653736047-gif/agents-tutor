# Phase 0-3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use
> `subagent-driven-development` or `executing-plans` task by task. Every behavior
> change follows test-first development.

**Goal:** 从当前统一 ReAct 内核继续推进，完成稳定运行时、会话持久化，以及无需真实语料也可测试的知识库/RAG 工具骨架。

**Architecture:** 保持四个 Agent 共用同一个 `ReActAgentNode`。图层负责路由和会话，工具层负责权限与执行，知识层通过小型协议隔离解析、分块、索引和检索实现。

**Tech Stack:** Python 3.11、LangGraph、LangChain Core、LangChain OpenAI、Pydantic、pytest。

---

### Task 0: 固化统一 ReAct 基线

**Files:**
- Create: `README.md`
- Create: `.env.example`
- Create: `backend/uv.lock`
- Modify: `backend/pyproject.toml`
- Modify: `backend/src/core/state.py`
- Create: `backend/src/core/nodes/`
- Create: `backend/src/core/tools/`
- Create: `backend/src/core/models/`
- Create: `backend/src/core/graph_builder.py`

- [ ] 确认 `.env` 被忽略且 `.env.example` 不包含密钥。
- [ ] 生成 `uv.lock`。
- [ ] 运行 `pytest tests -q -p no:cacheprovider`，预期全部通过。
- [ ] 运行 `ruff check --no-cache src tests scripts`，预期零问题。
- [ ] 运行 `mypy src/core`，预期零问题。
- [ ] Review 变更后提交 `feat: 统一多智能体 ReAct 架构` 并推送。

### Task 1: 补强运行时边界与事件

**Files:**
- Create: `backend/src/core/events.py`
- Create: `backend/src/core/tools/registry.py`
- Modify: `backend/src/core/state.py`
- Modify: `backend/src/core/nodes/react_agent.py`
- Modify: `backend/src/core/nodes/factory.py`
- Modify: `backend/src/core/graph_builder.py`
- Create: `backend/tests/test_events.py`
- Create: `backend/tests/test_tool_registry.py`
- Modify: `backend/tests/test_react_agent.py`
- Modify: `backend/tests/test_graph_builder.py`

- [ ] 先写失败测试，覆盖非法迭代数、图切换上限、非法路由和错误分类。
- [ ] 实现最小运行事件：Agent 开始/完成、工具开始/完成、切换、失败。
- [ ] 实现工具名唯一校验和按角色授权，不引入动态插件系统。
- [ ] 全量验证与 review 后提交 `feat: 强化 ReAct 运行时安全与事件` 并推送。

### Task 2: 会话持久化与上下文管理

**Files:**
- Modify: `backend/pyproject.toml`
- Modify: `backend/src/core/graph_builder.py`
- Create: `backend/src/core/sessions.py`
- Create: `backend/src/core/context.py`
- Create: `backend/tests/test_sessions.py`
- Create: `backend/tests/test_context.py`
- Modify: `backend/tests/test_graph_builder.py`

- [ ] 先写失败测试，覆盖同会话恢复、不同会话隔离和进程内重建恢复。
- [ ] 接入 SQLite Checkpointer，将 `session_id` 映射为 LangGraph `thread_id`。
- [ ] 实现轻量会话列表、历史读取和归档。
- [ ] 实现保留完整 Tool Call/ToolMessage 对的消息窗口。
- [ ] 全量验证与 review 后提交 `feat: 增加会话持久化与上下文管理` 并推送。

### Task 3: 知识库与 RAG 工具骨架

**Files:**
- Create: `backend/src/core/knowledge/models.py`
- Create: `backend/src/core/knowledge/loaders.py`
- Create: `backend/src/core/knowledge/chunking.py`
- Create: `backend/src/core/knowledge/index.py`
- Create: `backend/src/core/knowledge/service.py`
- Create: `backend/src/core/knowledge/tools.py`
- Create: `backend/tests/test_knowledge_*.py`

- [ ] 先写失败测试，定义文档、分块、检索结果和引用的数据契约。
- [ ] 实现纯文本/PDF 加载接口与可单测的分块函数。
- [ ] 定义索引协议并提供无需外部语料的内存实现。
- [ ] 封装写入、删除、Top-K 检索以及 `search_knowledge` 工具。
- [ ] 对空知识库返回明确结果，禁止生成虚假引用。
- [ ] 全量验证与 review 后提交 `feat: 搭建知识库与 RAG 工具骨架` 并推送。

### Final Verification

- [ ] 全量 pytest、Ruff、mypy 通过。
- [ ] 真实 DeepSeek ReAct smoke test 通过且不输出密钥。
- [ ] 独立 reviewer 检查阶段 0～3 的规格覆盖和代码质量。
- [ ] 对照用户要求确认四次提交均已推送至 `origin/soldier`。
